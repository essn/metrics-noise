apiVersion: v1
kind: Service
metadata:
  name: metrics-noise
  namespace: metrics-noise
spec:
  selector:
    app: metrics-noise
  ports:
    - name: http
      port: 4000
      targetPort: 4000
  type: ClusterIP
